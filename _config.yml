baseurl: /git-blog
exclude: [src]
defaults:
  -
    scope:
      #path: ""   # an empty string here means all files in the project
      type: "posts"   # or "pages"
    values:
      author: Dan Bernier
      layout: posts
